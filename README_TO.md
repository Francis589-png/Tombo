# `to` — Tombo package manager (prototype)

Usage examples:

```sh
python tools/to.py init mypkg
python tools/to.py publish mypkg
python tools/to.py list
python tools/to.py install mypkg
```

Start the REPL prototype:

```sh
python src/cli/repl.py
```

This is a minimal prototype to manage local packages in `./packages` and install into `./vendor`.
